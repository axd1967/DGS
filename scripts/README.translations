To use the translation scripts here just cd to the base directory of
DragonGoServer and do

$ scripts/get_update.sh

The result should be placed in include/translation_info.php instead of
the contents of the old $translation_info array.
